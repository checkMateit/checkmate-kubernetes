apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: store
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: store=ghcr.io/checkmate/checkmate-store
    argocd-image-updater.argoproj.io/store.update-strategy: newest-build
    argocd-image-updater.argoproj.io/store.helm.image-name: image.repository
    argocd-image-updater.argoproj.io/store.helm.image-tag: image.tag
spec:
  project: default
  source:
    repoURL: https://github.com/checkMateit/checkmate-kubernetes.git
    targetRevision: HEAD
    path: charts/store
    helm:
      valueFiles:
        - ../../values/dev/store.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: store
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true